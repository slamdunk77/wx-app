<!-- <view>{{aaa}}</view> -->
<view class="box">
  <!-- <view class="topbarcon">
    <view wx:for="{{tabs}}" 
    wx:key="id" 
    class="con {{item.isActive?'active':''}}"
    bindtap="handleItemTap"
    data-index="{{index}}"
    >
      {{item.name}}
    </view>
  </view>  -->
  <view class="maincon">
    <slot></slot>
  </view>
</view>